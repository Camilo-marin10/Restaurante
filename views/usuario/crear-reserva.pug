extends ../layout/index

block contenido
    div(class="py-10 px-4 sm:px-6 lg:px-8")
        h1(class="text-3xl font-extrabold text-gray-900 mb-6")= titulo
        p(class="text-lg text-gray-700") Por favor, selecciona los detalles de tu reserva.

        if errores
            div(class="p-4 mb-4 bg-red-50 border-l-4 border-red-500 text-red-700" role="alert")
                p(class="font-bold") Errores de Validación:
                ul(class="mt-1 list-disc list-inside")
                    each error in errores
                        li= error.msg

        form(class="mt-8 space-y-6" method="POST" action="/usuario/reserva/crear")
            input(type="hidden" name="_csrf" value=csrfToken)

            div(class="grid grid-cols-1 gap-6 md:grid-cols-3")
                div
                    label(class="block text-sm font-medium text-gray-700" for="fecha_reserva") Fecha
                    input#fecha_reserva(
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                        type="date" 
                        name="fecha_reserva" 
                        value=datos.fecha_reserva || "" 
                        min=new Date().toISOString().split('T')[0]
                    )
                
                div
                    label(class="block text-sm font-medium text-gray-700" for="hora_reserva") Hora Deseada

                    input#hora_reserva(
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" 
                        type="time" 
                        name="hora_reserva" 
                        value=datos.hora_reserva || ""
                        step="900" 
                    )

                div
                    label(class="block text-sm font-medium text-gray-700" for="numero_personas") Cantidad de Personas
                    input#numero_personas(
                        class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500"
                        type="number" 
                        name="numero_personas" 
                        value=datos.numero_personas || "" 
                        min="1" 
                        max="15"
                    )

            div(class="grid grid-cols-1 gap-6 md:grid-cols-3")
                div
                    label(class="block text-sm font-medium text-gray-700" for="duracion_estimada") Duración de la Reserva
                    select#duracion_estimada(class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" name="duracion_estimada")
                        option(value="") -- Selecciona Duración --
                        each duracion in duraciones
                            option(value=duracion.valor selected=(datos.duracion_estimada === duracion.valor.toString()))= duracion.texto

                div(class="col-span-2") 
                    
            div
                label(class="block text-sm font-medium text-gray-700" for="notas") Observaciones (Ej. Alergias, Cumpleaños
                textarea#notas(class="mt-1 block w-full rounded-md border-gray-300 shadow-sm focus:border-indigo-500 focus:ring-indigo-500" name="notas")= datos.notas || ""

            div
                button(
                    class="w-full flex justify-center py-2 px-4 border border-transparent rounded-md shadow-sm text-sm font-medium text-white bg-indigo-600 hover:bg-indigo-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-indigo-500"
                    type="submit"
                ) Confirmar y Buscar Disponibilidad