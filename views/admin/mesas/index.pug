extends ../../layout/index

block contenido
    div(class="py-12 px-4 sm:px-6 lg:px-8 bg-gray-50")
        h1(class="text-4xl font-extrabold text-red-800 mb-2 tracking-tight")= titulo
        p(class="text-lg text-gray-700 mb-8 border-b pb-4") Administra las mesas disponibles para reservas.
        
        div(class="flex justify-end mb-6")
            a(href="/admin/mesas/crear" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg uppercase text-sm transition duration-300 shadow-md") Crear Mesa

        
        if mesas.length
            div(class="bg-white shadow-2xl rounded-xl overflow-hidden border border-gray-100")
                div(class="overflow-x-auto")
                    table(class="min-w-full divide-y divide-gray-200")
                        thead(class="bg-red-50")
                            tr
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Nombre
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Capacidad
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Zona
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Estado
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Acciones
                        
                        tbody(class="bg-white divide-y divide-gray-100")
                            each mesa in mesas
                                tr(class="hover:bg-amber-50 transition duration-150")
                                    td(class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900") #{mesa.nombre}
                                    td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-600") #{mesa.capacidad}
                                    td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-600") #{mesa.zona}
                                    td(class="px-6 py-4 whitespace-nowrap")
                                        - const estadoClase = mesa.estado ? 'bg-green-100 text-green-800' : 'bg-red-100 text-red-800'
                                        - const estadoTexto = mesa.estado ? 'Activa' : 'Inactiva'
                                        span(class=`${estadoClase} px-3 py-1 inline-flex text-xs leading-5 font-bold rounded-full shadow-sm`) #{estadoTexto}

                                    td(class="px-6 py-4 whitespace-nowrap text-sm font-medium flex gap-4")
                                        a(href=`/admin/mesas/editar/${mesa.id}` class="text-red-600 hover:text-red-800 font-semibold transition duration-150")
                                            | Editar
                                        
                                        form(method="POST" action=`/admin/mesas/eliminar/${mesa.id}` onsubmit="return confirm('¿Estás seguro de que quieres ELIMINAR esta mesa? Esta acción es irreversible y afectará a las reservas asociadas.');")
                                            input(type="hidden" name="_csrf" value= csrfToken)
                                            button(type="submit" class="text-gray-500 hover:text-red-900 font-semibold transition duration-150")
                                                | Eliminar
        else
            div(class="text-center py-10 bg-white rounded-xl shadow-md border-t-4 border-orange-500")
                p(class="text-gray-600 text-lg mb-4") No hay mesas registradas en el sistema. 
                a(href="/admin/mesas/crear" class="text-red-600 hover:text-red-700 font-bold transition duration-300") ¡Crea la primera mesa ahora!