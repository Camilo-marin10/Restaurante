extends ../layout/index

block contenido
    div(class="py-10")
        h2(class="text-3xl font-bold text-gray-900 mb-6") Panel de Control

        div(class="grid grid-cols-1 md:grid-cols-3 gap-6 mb-10")
            // Widget: Total de Reservas Hoy (Solo cuenta las de hoy)
            div(class="bg-white overflow-hidden shadow rounded-lg p-5")
                dt(class="text-sm font-medium text-gray-500") Total de Reservas Hoy
                dd(class="mt-1 text-3xl font-semibold text-indigo-600") #{reservasHoy}
            
            // Widget: Capacidad Ocupada (Solo cuenta las de hoy)
            div(class="bg-white overflow-hidden shadow rounded-lg p-5")
                dt(class="text-sm font-medium text-gray-500") Capacidad Ocupada (Personas)
                dd(class="mt-1 text-3xl font-semibold text-indigo-600") #{capacidadOcupada}
            
            // Widget: No-Shows Hoy (Solo cuenta las de hoy)
            div(class="bg-white overflow-hidden shadow rounded-lg p-5")
                dt(class="text-sm font-medium text-gray-500") No-Shows Hoy
                dd(class="mt-1 text-3xl font-semibold text-red-600") #{noShowsHoy}

        // Tabla de Reservas
        div(class="bg-white shadow rounded-lg overflow-hidden")
            div(class="px-4 py-5 sm:px-6")
                h3(class="text-lg leading-6 font-medium text-gray-900") Próximas Reservas (5 Días)
                p(class="mt-1 max-w-2xl text-sm text-gray-500") Listado de reservas desde hoy, ordenadas por fecha y hora.
            
            div(class="border-t border-gray-200")
                if reservasDelDia.length
                    div(class="overflow-x-auto")
                        table(class="min-w-full divide-y divide-gray-200")
                            thead(class="bg-gray-50")
                                tr
                                    // 🚨 ENCABEZADO CORREGIDO PARA MOSTRAR FECHA Y HORA 🚨
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Fecha y Hora
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Cliente
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Personas
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Mesa
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Estado
                                    th(class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase") Acciones
                            
                            tbody(class="bg-white divide-y divide-gray-200")
                                each reserva in reservasDelDia
                                    tr
                                        // Muestra la fecha y hora concatenadas que vienen del controlador
                                        td(class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900") #{reserva.hora}
                                        td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-500") #{reserva.cliente}
                                        td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-500") #{reserva.personas}
                                        td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-500") #{reserva.mesa}
                                        td(class="px-6 py-4 whitespace-nowrap")
                                            // Estilos condicionales para el estado
                                            - const estadoClase = reserva.estado === 'Confirmada' ? 'bg-green-100 text-green-800' : reserva.estado === 'En Curso' ? 'bg-blue-100 text-blue-800' : reserva.estado === 'Cancelada' || reserva.estado === 'No-Show' ? 'bg-red-100 text-red-800' : 'bg-yellow-100 text-yellow-800'
                                            span(class=`${estadoClase} px-2 inline-flex text-xs leading-5 font-semibold rounded-full`) #{reserva.estado}
                                        
                                        
                                        td(class="px-6 py-4 whitespace-nowrap text-sm font-medium")
                                            a(class="text-indigo-600 hover:text-indigo-900" href=`/admin/reservas/editar/${reserva.id}`) Gestionar
                                            span(class="mx-2") |
                                            
                                            
                                            form(method="POST" action=`/admin/reservas/estado/${reserva.id}` class="inline-block")
                                                input(type="hidden" name="_csrf" value= csrfToken)
                                                
                                                input(type="hidden" name="nuevoEstado" value="No-Show") 
                                                button(type="submit" class="text-red-600 hover:text-red-900") No-Show
                else
                    div(class="p-4 text-center text-gray-500") No hay reservas activas en los próximos 5 días.