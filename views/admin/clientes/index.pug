extends ../../layout/index

block contenido
    div(class="py-12 px-4 sm:px-6 lg:px-8 bg-gray-50")
        h1(class="text-4xl font-extrabold text-red-800 mb-2 tracking-tight")= titulo
        p(class="text-lg text-gray-700 mb-8 border-b pb-4") Base de datos de clientes registrados para reservas.
        
        div(class="flex justify-between items-center mb-6")
            h3(class="text-xl font-bold text-gray-700") Total de Clientes: #{clientes.length}
            a(href="/admin/clientes/crear" class="bg-red-600 hover:bg-red-700 text-white font-bold py-2 px-4 rounded-lg uppercase text-sm transition duration-300 shadow-md") Registrar Nuevo Cliente

        if clientes.length
            div(class="bg-white shadow-2xl rounded-xl overflow-hidden border border-gray-100")
                div(class="overflow-x-auto")
                    table(class="min-w-full divide-y divide-gray-200")
                        thead(class="bg-red-50")
                            tr
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Nombre Completo
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Correo Electrónico
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Teléfono
                                th(class="px-6 py-3 text-left text-xs font-bold text-red-700 uppercase tracking-wider") Acciones
                        
                        tbody(class="bg-white divide-y divide-gray-100")
                            each cliente in clientes
                                tr(class="hover:bg-amber-50 transition duration-150")
                                    td(class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900") #{cliente.nombre} #{cliente.apellido}
                                    
                                    td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-600")
                                        | #{cliente.email || 'N/A'}
                                        
                                    td(class="px-6 py-4 whitespace-nowrap text-sm text-gray-600")
                                        | #{cliente.telefono || 'N/A'}

                                    td(class="px-6 py-4 whitespace-nowrap text-sm font-medium flex gap-4")
                                        a(href=`/admin/clientes/editar/${cliente.id}` class="text-red-600 hover:text-red-800 font-semibold transition duration-150")
                                            | Editar
                                        
                                        form(method="POST" action=`/admin/clientes/eliminar/${cliente.id}` onsubmit="return confirm('¿Estás seguro de que quieres ELIMINAR a este cliente? Esto podría afectar reservas anteriores.');")
                                            input(type="hidden" name="_csrf" value= csrfToken)
                                            button(type="submit" class="text-gray-500 hover:text-red-900 font-semibold transition duration-150")
                                                | Eliminar
        else
            div(class="text-center py-10 bg-white rounded-xl shadow-md border-t-4 border-orange-500")
                p(class="text-gray-600 text-lg mb-4") No hay clientes registrados en la base de datos. 
                a(href="/admin/clientes/crear" class="text-red-600 hover:text-red-700 font-bold transition duration-300") ¡Registra el primer cliente ahora!